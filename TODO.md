# PawMatch — TODO (P2, deferred)

Items intentionally scoped out of the first MVP revision pass. Pulled from
`pawmatch-mvp-revision-prompt.md` § P2.

- [ ] **Shelter profile screens** — linked from match rows and chat headers
- [ ] **Application status timeline** inside match rows
      (Submitted → Reviewing → Meet & greet)
- [ ] **Filter-state persistence** via localStorage (swipe filter + onboarding answers)
- [ ] **Real map integration** (Mapbox / MapLibre) with branded pins, replacing
      the round-1 stylized SVG map in `VetFindScreen`
- [ ] **"I'll do this later" path on insurance** that lands on swipe with an
      `Insurance pending` badge in Profile
